@model IEnumerable<AdvertisingCompany.Models.ResponsibleOfficer>

@{
    Layout = "_Layout";
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameResponsibleOfficer)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Adress)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PhoneNumber)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameResponsibleOfficer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResponsibleOfficerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResponsibleOfficerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResponsibleOfficerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
